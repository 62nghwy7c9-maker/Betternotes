# Opus's lessons journal

Newest first. Format:

```
## YYYY-MM-DD — <one-line lesson, as an instruction to future-you>
Context: <what happened, what it cost>
Scope: repo | general
```

Entries marked `(promoted)` have been incorporated into a SKILL.md,
`.claude/agents/opus.md`, or the project CLAUDE.md.

---

## 2026-07-06 — Seed entry from Fable: record the correction, not the compliment.
Context: The strongest signal you'll ever get is a user or a red test telling
you that you were wrong. Praise teaches nothing; corrections are the entire
curriculum after this point. This entry exists so the journal is never empty
and the format is never in doubt.
Scope: general
